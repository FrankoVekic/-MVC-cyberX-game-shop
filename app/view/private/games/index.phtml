      <div style="margin-top: 150px; margin-bottom:100px;" class="album py-5">
      <div class="container">
          <div class="row">
          <div class="form-outline" style="margin-bottom: 30px;">
            <form action="<?=App::config('url')?>games/index">
            <input style="text-align: center;" type="text" name="search" id="search" class="form-control" 
            placeholder="What are you searching for?" value="<?=$search?>"/>
            </form>
          </div>
          <?php include 'paging.phtml' ?>
          <?php foreach($games as $game):?>
            <div class="col-md-4">
            <h3><?=$game->name?></h3>
              <div class="card mb-4 box-shadow">
                <img class="games-img" src="<?=App::config('url')?>public/img/order/<?=$game->image?>" alt="Card image cap">
                <div class="card-body">
                <p class="card-text">Price: <?=$game->price?>$</p>
                <p class="card-text">Console: <?=$game->console?></p>
                <p class="card-text">Memory required: <?=$game->memory_required?> GB</p>
                  <p class="card-text"><?=$game->description?></p>
                  <div class="d-flex justify-content-between align-items-center">
                    <form action="<?=App::config('url')?>games/add" method="POST">
                        <input type="hidden" name="id" value="<?=$game->id?>">
                        <input style="margin:10px;" type="number" name="quantity" value="1" min="1" max="<?=$game->quantity?>" placeholder="Quantity" required>
                        <button type="submit" name="add" class="btn btn-primary">Add to Cart</button> 
                      </form>
                      <?php if(Request::isAdmin()): ?>
                      <a href="<?=App::config('url')?>games/change?id=<?=$game->id?>"> <i title="Change" class="fas fa-2x fa-pencil-alt"></i></a>
                      <?php endif;?>
                  </div>
                </div>
              </div>
            </div>
            <?php endforeach;?>
            <?php include 'paging.phtml' ?>
            </div>
          </div>
        </div>

